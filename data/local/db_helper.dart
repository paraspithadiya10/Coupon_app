class DbHelper {
  /// Singleton
  DbHelper._();
  static final DbHelper getInstance = DbHelper._();

  Database? myDB;

  /// db Open(path -> if exist -> open, if not exist -> create)

  /// all queries
}
